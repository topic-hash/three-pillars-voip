fn main() {
    // Pure proc-macro approach: no UDL generation needed.
    // The uniffi::setup!() macro in lib.rs handles scaffolding generation.
    // The voip.udl file is a reference specification only.
}
