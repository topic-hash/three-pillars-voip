# ARMv7 native library
#
# This directory is populated by `cargo ndk`:
#   cargo ndk -t armeabi-v7a -o mobile/android/src/main/jniLibs build --release -p voip-ffi
#
# The compiled libvoip_ffi.so goes here.
