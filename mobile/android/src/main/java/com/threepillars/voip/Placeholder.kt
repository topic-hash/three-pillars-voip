package com.threepillars.voip

/**
 * Three Pillars VoIP — Kotlin API
 *
 * This file is a placeholder. The actual Kotlin bindings are generated
 * by UniFFI (`uniffi-bindgen generate --language kotlin`) from the compiled
 * Rust library (libvoip_ffi.so).
 *
 * After running the build, this directory will contain:
 *   - UniFFI-generated data types (VoipConfig, VoipClient, etc.)
 *   - FFI callback interfaces
 *   - Object wrappers for Rust types
 *
 * See README.md for full build instructions.
 */
